// Schema lives in core (referenced by the AnyNode union). Re-export so
// every turbine-vent-related import stays inside @pascal-app/nodes/turbine-vent.
export { TurbineVentNode } from '@pascal-app/core'
